import React from 'react';

const ReadyToursModule = () => {
    return (
        <div>
            тут мы пишем уже jsonData.map(() => CardTour)
        </div>
    );
};

export default ReadyToursModule;